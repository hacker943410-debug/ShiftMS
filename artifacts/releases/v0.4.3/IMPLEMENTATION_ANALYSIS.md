# v0.4.3 구현 분석

## 변경 축
- renderer: 근무지 등록 1단계 조별 Index 배치와 카드 폭을 조정한다.
- main/shared: Access 근무실적 복원 시 시급 미복원/필수값 누락 행의 복원 정책을 보강한다.
- release: `package.json` 버전과 릴리즈 문서를 `0.4.3`으로 갱신한다.

## 제약
- Pool 대체근무는 기존과 동일하게 수당 이력에서 제외해야 한다.
- 이번 패키징 턴에서는 사용자 요청에 따라 `npm run test`와 smoke를 생략한다.
