# v0.4.17 QA Checklist

## 자동 검증 완료
- [x] 시작 시 새 업데이트가 있으면 다운로드 전에 사용자가 먼저 알림을 볼 수 있다.
- [x] 업데이트 다운로드는 사용자가 다운로드 버튼을 누르기 전에는 시작되지 않는다.
- [x] 패치노트 매니페스트가 표 기반 섹션을 포함하고 파싱된다.
- [x] 같은 시급과 같은 총 시간의 수당은 항목 분할과 무관하게 원시 합산 후 1회 올림 금액으로 계산된다.
- [x] 요율 영향 미리보기는 실제 수당 계산과 같은 1회 올림 기준을 사용한다.
- [x] 승인 완료 파일을 승인 대기 폴더로 이동한 뒤 부분 승인 상태를 재조회하는 시나리오가 통과한다.
- [x] 품의 승인 후 품의서, 별첨1, 별첨2 Excel 출력 테스트가 통과한다.
- [x] 품의서/별첨 Excel 병합 셀 정리 회귀 테스트가 통과한다.
- [x] 양식 스타일 병합 적용 후 중복 병합 오류 회귀 테스트가 통과한다.
- [x] DB 교체 중단 후 migration backup만 남은 상황의 복구 테스트가 통과한다.
- [x] 월 근무표 저장 실패 시 기존 저장 데이터가 유지되는 테스트가 통과한다.
- [x] 요율 매트릭스 비정상 배율 저장 방지 테스트가 통과한다.
- [x] 요율 영향 미리보기와 0원화 경고 테스트가 통과한다.
- [x] 근무표 규칙 경고 테스트가 통과한다.
- [x] 인력관리 하단 집계가 현재 필터링된 목록 기준과 일치하는 테스트가 통과한다.
- [x] `npm run build`가 통과한다.
- [x] 전체 `npm run test`가 통과한다. (`127 files / 554 tests`)

## 수동 QA 필요
- [ ] 설치본 실행 직후 새 버전이 있을 때 다운로드 전에 패치노트가 먼저 표시된다.
- [ ] 업데이트 모달에서 기능 중심 표 패치노트를 읽은 뒤 다운로드를 시작할 수 있다.
- [ ] 품의승인 완료 상태에서 품의서 Excel을 재출력해도 병합 셀 오류가 발생하지 않는다.
- [ ] 별첨1 Excel 재출력 후 파일을 다시 열 수 있다.
- [ ] 별첨2 Excel 재출력 후 파일을 다시 열 수 있다.
- [ ] renderer 화면 오류가 앱 전체 white-screen으로 확산되지 않는다.
- [ ] 운영 설정에서 근무표 경고 기준 저장 후 재조회 값이 유지된다.
- [ ] 근무표 화면에서 경고가 표시되지만 배포 실행은 차단하지 않는다.
- [ ] 요율 영향 미리보기에서 총액 변동과 0원화 경고를 확인할 수 있다.

## 제외 확인
- [ ] Electron navigation/window-open/CSP 제한은 이번 패치에서 적용하지 않는다.
- [ ] 인증 bootstrap 정책 변경은 이번 패치에서 적용하지 않는다.
