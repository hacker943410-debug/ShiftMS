# V0.2.0 QA 체크리스트

## 작성 기준
- 자동 검증으로 확인된 항목은 `[x]`
- 실제 화면 조작 또는 현장 샘플 검증이 필요한 항목은 `[ ]`

## 1. 시급 업데이트 일괄 적용

### 1.1 화면 / 조작 확인
- [ ] 인력 관리 화면에서 `시급 일괄 업데이트` 버튼이 보인다.
- [ ] 버튼 클릭 시 모달이 열린다.
- [ ] Excel 파일 선택이 가능하다.
- [ ] 컬럼 매핑 입력이 가능하다.
- [ ] 적용일 선택이 가능하다.
- [ ] `가이드 보기` 버튼 클릭 시 설명 모달이 열린다.

### 1.2 서비스 / 데이터 자동 검증
- [x] 유효한 파일 + 매핑으로 preview 데이터가 생성된다.
- [x] 적용 가능 행 수와 제외 행 수가 집계된다.
- [x] 중복 행은 `duplicate-entry`로 제외된다.
- [x] 동일 금액 행은 저장 대상에서 제외된다.
- [x] 존재하지 않는 인력은 `employee-not-found`로 제외된다.
- [x] 잘못된 시급 값은 `invalid-hourly-rate`로 제외 사유가 생성된다.

### 1.3 저장 결과 자동 검증
- [x] 적용 가능 행만 저장된다.
- [x] 기존 활성 시급 종료일이 적용일 전날로 자동 반영된다.
- [x] 새 시급 이력의 시작일이 적용일로 저장된다.
- [ ] 인력 상세의 `현재 시급`이 최신 값으로 바뀐다.
- [ ] `시급변경이력`에 새 레코드가 UI에서 확인된다.
- [ ] 이력 사유에 일괄 업데이트 흔적이 남는지 UI에서 확인된다.

### 1.4 예외 / 수동 확인
- [x] 근무지명 불일치 행이 제외된다.
- [ ] 이름 중복 매칭 행이 제외된다.
- [ ] 퇴사자 행이 제외된다.
- [ ] 적용일이 너무 이른 행이 제외된다.
- [ ] 빈 행이 무시된다.
- [ ] 적용 전 / 적용 후 시급 비교가 UI에 올바르게 보인다.

## 2. 패턴 산출 및 적용

### 2.1 화면 / 조작 확인
- [ ] 근무지 관리 화면에서 `패턴 적용된 근무지 추가` 버튼이 보인다.
- [ ] 버튼 클릭 시 분석 모달이 열린다.
- [ ] 파일 선택이 가능하다.
- [ ] `가이드 보기` 버튼 클릭 시 표준 템플릿 안내가 열린다.
- [ ] 분석 실행이 가능하다.
- [ ] `분석 결과`, `그룹별 상세`, `불일치 내역`, `원본 데이터` 탭 전환이 가능하다.
- [ ] `텍스트 복사` 버튼이 동작한다.

### 2.2 서비스 / 알고리즘 자동 검증
- [x] 표준 템플릿 파일을 정상 파싱한다.
- [x] 분석 기간, 총 일수, 근무자 수가 계산된다.
- [x] 공휴일 수와 고유 근무코드 목록이 분석 결과에 포함된다.
- [x] Cycle 수, 팀 수, 정원 제안이 생성된다.
- [x] 대표 패턴 문자열이 제안 데이터에 포함된다.
- [x] rotation 기준 그룹 분류가 동일 Cycle을 묶는다.
- [x] 분석 결과 텍스트가 그룹 요약과 함께 생성된다.
- [x] 불일치 내역에 기준일 index와 cycle index가 포함된다.
- [ ] 경고가 있으면 결과 데이터에 포함되는지 운영 샘플로 확인한다.
- [ ] 패턴 미발견 시 오류가 UI에서 올바르게 표시된다.

### 2.3 Site draft 적용 확인
- [ ] `근무지 등록(1단계 이동)` 클릭 시 Site 1단계로 이동한다.
- [ ] 팀 수가 산출 결과대로 draft에 채워진다.
- [ ] Cycle 수가 산출 결과대로 draft에 채워진다.
- [ ] 패턴 문자열과 시작일이 자동 입력된다.
- [ ] 조별 Cycle 배정이 자동 입력된다.
- [ ] 조별 정원이 자동 입력된다.
- [x] 아직 저장 전에는 기존 근무지 데이터가 변경되지 않는다.

### 2.4 후속 검토
- [ ] 사용자가 근무시간과 휴게시간을 수정할 수 있다.
- [ ] 달력 시뮬레이션이 정상적으로 렌더링된다.
- [ ] 저장 후 상세 보기에서 Cycle, 팀 인덱스, 정원이 올바르게 보인다.

## 3. 회귀 점검
- [ ] 개별 시급 변경 기능이 기존처럼 동작한다.
- [ ] 신규 인력 등록 기능이 기존처럼 동작한다.
- [ ] 기존 근무지 등록 / 수정 / 삭제가 동작한다.
- [ ] 근무지 1단계 시뮬레이션이 기존처럼 동작한다.
- [ ] 2단계 조 배정 드래그 앤 드롭이 기존처럼 동작한다.

## 4. 자동 검증
- [x] `npm run typecheck`
- [x] `npm run test`
- [x] `node scripts/validate-structure.mjs`
- [x] `npm run build`

## 5. 자동 검증 메모
- `src/main/services/workforce-wage-bulk-update-service.test.ts`
  - 미리보기 집계, 제외 사유, 저장 후 시급 이력 날짜 반영 검증 완료
- `src/main/services/site-pattern-extraction-service.test.ts`
  - 표준 템플릿 파싱, 고유 코드/공휴일 집계, 텍스트 결과, mismatch cycle index, suggestion의 cycle/team/headcount/pattern 검증 완료
- `src/shared/domain/site-pattern-detection.test.ts`
  - 패턴 탐지 / rotation 그룹 분류 검증 완료

## 6. 결과 기록
- 확인 일시: `2026-03-30 15:47:36 +09:00`
- 확인자: `Codex`
- 차단 이슈: `없음`
- 보완 이슈:
  - 실제 운영 Excel 샘플 기준 UI 수동 검증 필요
  - Playwright 또는 수동 캡처 기준 화면 점검 필요
