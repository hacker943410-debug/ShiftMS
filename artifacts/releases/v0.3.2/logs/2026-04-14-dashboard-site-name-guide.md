# 2026-04-14 대시보드 월 선택 / 사이트 명 가이드 보강 로그

## 작업 범위
- 대시보드 `기간 직접 지정`의 `시작 월`, `종료 월` 입력을 브라우저 기본 `type="month"`에서 앱 공통 팝오버 톤의 `MonthField`로 교체했다.
- 운영 관리 가이드에 `사이트 명 관리` 페이지와 `site-name` 시뮬레이션 장면을 추가했다.
- v0.3.2 릴리즈 아카이브 인덱스, 패치노트, 파일 영향 범위, TODO, QA 체크리스트, 결과 보고를 현재 상태 기준으로 갱신했다.

## 확인
- `npm run typecheck` 통과
- `node scripts/validate-structure.mjs` 통과
- `npm run build:renderer` 통과

## 남은 확인
- 대시보드 월 선택 팝오버의 실제 화면 수동 확인
- 운영 관리 `사이트 명 관리` 가이드 화면 수동 확인
