# 통상시급 · 입사일 · 인력관리 규칙 대장

> 기준 버전 **0.5.4** (조사일 2026-09-04). 줄번호는 그 시점 기준이며, 코드를 고치면 같이 고칠 것.
> 조사 방법: 10개 영역 병렬 조사 → 항목 100건을 원본과 재대조(77건 정정) → 결함 지목분을 관점 2개로 반박 시도(12건 반박 탈락).
>
> **✅ 표시가 붙은 함정은 고쳐졌다**(1차 패치, 2026-09-04). 아직 게시되지 않았으므로 **운영자가 쓰는 0.5.4 배포판에는 그대로 남아 있다.**

## 0. 질문 → 찾아갈 항목

| 이런 질문이 오면 | 볼 곳 |
|---|---|
| "신규 등록할 때 입사일은 언제로 넣어야 해요?" | R-1 ~ R-5 |
| "시급이 언제부터 적용되나요?" | R-6 ~ R-9 |
| "시급을 고쳤는데 금액이 안 바뀌어요" | **T-1** (가장 흔함) |
| "이미 승인한 게 다시 승인 대기로 떠요" | **T-2** |
| "화면에 보이는 시급이랑 실제 금액이 달라요" | **T-3** |
| "지난 날짜로 넣었는데 경고가 안 떴어요" | **T-4** |
| "일괄 업데이트했는데 몇 명이 빠졌어요" | **T-7 ~ T-11** (사번 열을 채우면 대부분 해결) |
| "미리보기랑 실제 적용된 게 달라요" | **T-9 · T-18-1** |
| "입사일을 잘못 넣었는데 못 고쳐요" | **T-12** |
| "입사일 이전(퇴사 뒤) 근무라며 승인이 안 돼요" | **T-22** (인력 관리에서 날짜를 고치면 다시 읽힌다) |
| "배정/시급을 입사일보다 앞 날짜로 못 넣어요" · "전보자가 1일부터 근무표에 들어가요" | **T-23** |
| "시급이 0원이라 승인이 안 돼요" | R-11, T-14 |
| "옛날 달 실적을 다시 불러오면 시급을 못 찾아요" | R-11, T-15 |
| 실제 저장된 시급 장부가 성한지 보고 싶다 | `artifacts/scripts/diagnose-wage-history.cjs` |

---

## 1. 규칙 (설계대로 동작하는 것)

### R-1. 입사일은 신규 등록 창에서만 입력하고, 필수이며, 기본값은 오늘
등록 창을 열면 입사일이 **오늘(한국 날짜)** 로 채워진다. 비우면 저장되지 않는다.
- **운영자에게**: 등록할 때 입사일을 반드시 넣어야 하고, 안 고치면 오늘 날짜로 들어갑니다.
- **근거**: `WorkforceManagementScreen.tsx:692-697`(모달 열 때 오늘로 초기화) · `:855-866`(필수 검증) · `:2195-2203`(입력칸) · `shared/lib/local-date.ts:5-8`(오늘 = 로컬 날짜, UTC 아님)

### R-2. 등록 창에 넣은 통상시급의 적용 시작일은 '오늘'이 아니라 '입사일'이다
등록 시 시급을 함께 넣으면 시급 이력 첫 줄이 자동 생성되는데, 그 적용일은 입사일이다. 종료일은 비어 있고(계속), 사유는 `"직원 등록/수정"` 고정.
- **운영자에게**: 입사일을 과거로 넣으면 시급도 그 과거 날짜부터 소급 적용됩니다. 경고는 뜨지 않습니다.
- **근거**: `employee-storage-service.ts:557-584`, 적용일은 `:579` (`input.hireDate ?? updatedAt.slice(0,10)`)

### R-3. 첫 근무지 배정은 근무지와 근무조를 **둘 다** 골랐을 때만 생기고, 시작일은 입사일이다
근무조명이 공백이면 배정이 아예 만들어지지 않는다.
- **근거**: `employee-storage-service.ts:459-461`(조건) · `:550`(start_date = 입사일) · `shared/domain/team-label.ts:7-23`(공백 정규화)

### R-4. BP 인력은 등록 화면에서 사번·시급을 넣을 수 없다
BP를 고르면 두 칸이 잠기고 이미 입력한 값도 지워진다. 사번은 내부용으로 자동 생성되고, **시급 이력 0줄로 시작**한다. 단 이 제한은 화면에만 있고 저장 로직에는 없다.
- **근거**: `WorkforceManagementScreen.tsx:549-563`(값 지움) · `:868-871`(저장 요청에서 제외) · `:2132`·`:2260`(칸 잠금)

### R-5. 인력 표가 완전히 비었을 때만 시연용 기본 인력 3명이 생긴다
`2026-01-01`이라는 날짜는 그 시드 안에서만 쓰이는 예비값이며 실제 등록에는 관여하지 않는다.
- **근거**: `employee-storage-service.ts:152-256`(0명일 때만) · `:237`·`:249`

### R-6. 시급은 값 하나가 아니라 '적용일 ~ 종료일' 구간의 목록이다
종료일이 비어 있으면 계속 유효하다는 뜻.
- **근거**: `sqlite-storage-service.ts:92-100`(wage_rates 정의) · `:102-103`(인덱스는 유니크 아님 — 겹침을 DB가 막지 않는다)

### R-7. 저장 알고리즘 — 같은 날짜면 덮어쓰고, 앞줄은 전날로 끊고, 새 줄은 다음 줄 전날까지
1. 그 인력의 모든 시급 줄을 시작일 오름차순으로 읽는다.
2. 새 시작일을 걸치고 있는 앞줄(`시작일 < 새 시작일` 이고 `종료일이 비었거나 새 시작일 이후`)을 **새 시작일의 전날**로 끊는다. **시작일이 같은 줄을 고쳐 쓰는 경우에도 똑같이 끊는다** — 정상 이력에서는 끊을 줄이 없고(앞줄이 이미 전날에 끝나 있음), 옛 프로그램에서 넘어온 겹친 이력에서는 이것이 유일한 정리 경로다(R13 자체검증 전에는 이 경우 앞줄을 그대로 두어 겹침이 영영 남았다).
3. **시작일이 같은 줄이 있으면** 새 줄을 만들지 않고 그 줄(같은 시작일이 여럿이면 가장 나중에 만든 줄)의 금액·사유·종료일을 고쳐 쓴다.
4. 없으면 새 줄을 넣고, 종료일은 **다음 줄 시작일의 전날**(다음 줄이 없으면 비움).
- **운영자에게**: 그래서 기간이 겹치는 일은 구조적으로 생기지 않습니다. 대신 **같은 날짜로 다시 저장하면 이전 금액이 어디에도 남지 않습니다.** 이력 화면이 "앞 줄과 겹침"을 알리면 그 줄의 시작일로 다시 저장하세요(같은 시작일 줄 여러 개는 지울 수 없고 가장 나중 줄만 쓰입니다).
- **근거**: `employee-history-service.ts:375-444` — `:392`(같은 날짜 찾기) · `:394-395`(다음 줄) · `:399-411`(덮어쓰기) · `:416-422`(앞줄 끊기) · `:424-442`(새 줄 넣기)
- **시험**: `employee-history-service.test.ts:66`·`:95`·`:126`

### R-8. '그날의 시급' 판정은 시작일·종료일 **양끝 포함**, 겹치면 시작일이 가장 늦은 줄이 이긴다
목록이 시작일 내림차순으로 오고 위에서부터 첫 일치를 채택하기 때문.
- **근거**: `employee-history-service.ts:335`(정렬) · `schedule-return-performance-parser.ts:424-438` — `:426`(시작일 포함) · `:430`(종료일 포함) · `:437`(없으면 undefined)

### R-9. 실적 금액은 '근무한 날짜'의 시급으로 계산된다 (파일 불러온 날짜·월 기준 아님)
파일 하나당 인력별 시급 이력을 한 번 통째로 읽고, 실적 줄마다 그 근무일로 시급을 고른다. 월 중간에 시급이 바뀌면 같은 달 안에서도 날짜별로 다른 시급이 적용된다.
- **근거**: `schedule-return-performance-parser.ts:393`·`:402`(파일당 1회 조회) · `:809`(줄마다 근무일로 결정) · `:1187`(항목에 실림) · `performance-file-storage-service.ts:558`(DB 저장)

### R-10. 승인하는 순간 시급과 계산 결과가 통째로 보관되고, 이후 시급 변경은 승인분에 영향이 없다
- **운영자에게**: 이미 결재가 끝난 금액은 나중에 시급을 바꿔도 절대 다시 계산되지 않습니다.
- **근거**: `performance-approval-flow-service.ts:199`(스냅샷 저장) · `performance-approval-snapshot-service.ts:129-143` · `performance_approvals.snapshot_json`

### R-11. 시급이 없거나 0이면 금액 0원이 아니라 **승인 자체가 막힌다** (3중 관문)
① 읽을 때 오류 알림이 붙고 ② 승인 시 차단되고 ③ 계산에서도 걸린다. Pool 대체근무는 예외(시급을 요구하지 않음).
- **오류 문구**: `"○○○의 YYYY-MM-DD 기준 적용 시급을 찾지 못했습니다. 현재 등록 시작일: …"` / 이력이 없으면 `"○○○의 시급 이력이 없습니다"`
- **근거**: `schedule-return-performance-parser.ts:1139-1145` · `performance-approval-flow-service.ts:155-161`

### R-12. 금액 셈법 — 줄별로 구하고 합계에서 한 번만 올림
`(시급 × 분 × 배수) ÷ 60` 을 기본·연장·야간 각각 구한 뒤 **합계에서 딱 한 번 1원 단위 올림**. 10원 절사 없음. 배율(요율) 버전은 근무일 기준으로 고른다.
- **근거**: `shared/domain/rounding.ts` · `approved-allowance-calculation-service.ts:108-126`(요율 버전 선택) · `:128-147`

### R-13. 시급 줄에는 삭제가 없다
쓰기 기능은 **저장**과 **종료** 둘뿐. 삭제 API 자체가 없다(시험 초기화용 SQL 한 줄이 유일).
- **운영자에게**: 잘못 넣은 줄은 **같은 적용일로 다시 저장해 덮어쓰는 것**이 유일한 정정 방법입니다.
- **근거**: `shared/bridge/contracts.ts:936`·`:939` · `register-workforce-handlers.ts:90`·`:100` · `employee-storage-service.ts:669`(시험용 DELETE)

### R-14. 시급 표에는 '누가 바꿨는지'가 없다
칸은 7개(id·인력·금액·시작일·종료일·사유·생성시각). 등록자 칸도, 중복 방지 제약도, 외래키도 없다. 다만 **활동 이력에는 누가 언제가 남는다**. 같은 줄을 덮어쓸 때 생성시각은 갱신되지 않는다.
- **근거**: `sqlite-storage-service.ts:92-103` · `employee-history-service.ts:396`

### R-15. 엑셀 일괄 적용은 단건 저장과 같은 규칙을 쓰고, 한 덩어리 거래로 처리된다
[적용]은 화면의 미리보기를 쓰지 않고 **그 시점에 다시 판정**한 뒤, `ready`인 줄만 골라 `BEGIN~COMMIT` 안에서 단건 저장 함수를 그대로 호출한다. 하나라도 실패하면 전부 취소.
- **근거**: `workforce-wage-bulk-update-service.ts:397-436` — `:413`(재판정) · `:414`(ready만) · `:426`(저장)

### R-16. 일괄 적용 엑셀 읽기 규칙
- 첫 번째 시트만, **데이터는 무조건 2행부터**. 1행은 **머리글 줄로만** 쓰인다(데이터로 읽지 않는다).
- 읽는 칸은 **사번(선택)·근무지명·이름·시급 네 개**.
- **파일을 고르면 1행 머리글을 읽어 열을 자동으로 채운다**('사번'·'사원번호'·'근무지명'·'성명'·'통상시급' 등). 규칙 세 가지:
  1. **머리글이 이름을 댈 때만** 채운다. 자리(A열이니까 사번이겠지)로 넘겨짚지 않는다.
  2. **같은 이름의 머리글이 두 열에 있으면 그 칸을 비워 두고** 후보 열을 화면에서 알린다(옛 시급·새 시급이 나란히 있는 파일 대비). 필수 칸(근무지명·이름·시급)이 비어 있으면 **[미리보기] 자체가 잠긴다** — 운영자가 고르기 전에는 아무것도 못 돌린다.
  3. 못 찾은 항목은 **기본값(B/C/D)** 을 넣고 "확인하세요"라고 알린다. **직전 파일의 값을 물려주지 않는다.**
- 머리글 읽기 결과는 **그 파일 것일 때만** 반영된다. 파일을 연달아 고르거나, 운영자가 열을 손으로 고치거나, **이미 미리보기·적용이 시작됐으면** 늦게 도착한 옛 결과는 버려진다(매핑 세대 규칙, `reduceWageBulkMapping`).
- 사번 머리글이 없으면 비워 두고 **"사번 없이 찾으면 사람이 빠질 수 있다"** 고 경고한다. 열 글자는 언제든 손으로 고칠 수 있다.
- 금액은 쉼표·공백·'원'만 지우고 숫자로 변환. 0 이하는 오류, **소수점은 그대로 통과**.
- 같은 사람이 두 번 나오면 **첫 줄이 이긴다**.
- 적용일은 행별이 아니라 **모달 전역 하나**, 기본값 오늘.
- **근거**: `workforce-wage-bulk-update-service.ts:38`·`:72-82`·`:94-101`·`:204-226`·`:356-376` · `WorkforceManagementScreen.tsx:110-114`

### R-17. 일괄 적용 미리보기 상태값 9종
`적용 가능` `적용 완료` `필수값 누락` `시급 형식 오류` `일치 인력 없음` `동일 인력 중복` `퇴사자 제외` `기존 시급과 동일` `중복 항목`.
'기존 시급과 동일'은 **오늘 시급이 아니라 고른 적용일 그 날에 유효했던 시급**과 비교한다.
- **근거**: `workforce-wage-bulk-update-service.ts:40-50`(문구) · `contracts.ts:406-415`(타입)

### R-18. 퇴사 처리는 시급 줄도 근무지 배정도 닫지 않는다
상태만 바뀐다. 퇴사일 이후 날짜로 시급을 등록하는 것도 막지 않는다. 같은 사번으로 재입사 등록은 **불가**(사번 중복 차단).
- **근거**: `WorkforceManagementScreen.tsx:1053-1063`(퇴사 저장 시 시급 미포함) · `employee-storage-service.ts:557`(시급이 숫자일 때만 손댐) · `:475-481`(사번 중복)

### R-19. 인력 상세의 '입사일'은 비어 있으면 근무지 배정 시작일을 대신 보여준다
3단 폴백(`입사일 → 현재 배정 시작일 → 최근 배정 시작일`)인데 라벨은 그냥 '입사일'이다.
- **근거**: `WorkforceManagementScreen.tsx:403-404` · `:1223`

### R-20. 옛 프로그램(Access) 이관 규칙 — 여기서 대부분의 과거 데이터 문제가 나온다
- **입사일**을 `직무적용일자 → 조직개편일자 → 그 사람 시급 시작일 → 파일연도-01-01` 순으로 **대신 채운다**. 진짜 입사일이 아니다.
- 시급은 사람당 **'적용유무=참'인 한 줄만** 옮긴다. 과거 시급 이력은 통째로 사라진다.
- 그 한 줄의 시작일은 실제 적용일이 아니라 **Access에 입력한 날짜**다.
- 이관된 실적의 시급은 Access 원본 금액 → 수당÷(시간×요율) 역산 → 사번 기준 폴백 순으로 채워진다.
- **근거**: `database-migration-service.ts:1180`·`:1189-1191`·`:1264-1266`·`:1287`·`:2048-2096`
- **시험 없음** — 이 압축 로직에는 자동 시험이 하나도 없다.

---

## 2. 함정 (화면 안내와 실제가 다르거나, 사고가 나는 곳)

### T-1. [치명] ✅고침(안내만) 시급을 고쳐도 이미 불러온 승인 대기 실적의 금액은 그대로다
시급은 실적 엑셀을 **처음 읽는 순간** 각 줄에 박혀 저장된다(R-9). 그 뒤 시급 장부를 고쳐도 이미 목록에 있는 줄은 저절로 바뀌지 않는다. 시급 변경은 재분석 표식을 전혀 남기지 않고, 엑셀 파일이 그대로면 저장된 옛 분석 결과를 재사용한다.
- **지금(패치 후)**: 안내 문구가 사실대로 말한다 — "자동으로 바뀌지 않습니다. 실적 관리 화면에서 새로고침(↻)으로 다시 읽어야 반영됩니다. 다만 그 파일에서 일부만 승인해 뒀다면 이미 승인한 줄도 재검토로 되돌아갑니다."(T-2 위험까지 함께 알린다)
- **아직 남은 것**: 자동 재계산은 넣지 않았다. 넣으면 T-2가 터진다. 운영자가 ↻를 직접 눌러야 한다.
- **옛 결함(0.5.4 배포판)**: `"아직 승인하지 않은 실적은 새 시급으로 다시 계산됩니다"` 라고 **반대로** 안내했다.
- **근거**: 문구 `WorkforceManagementScreen.tsx`의 `WAGE_CHANGE_REFRESH_NOTICE` · 시급이 실적에 박히는 지점 `schedule-return-performance-parser.ts:424-438` · `performance-file-storage-service.ts:558`
- **시험 없음** — 시급→실적금액 반영을 검증하는 자동 시험은 여전히 0건.

### T-2. [높음] ✅고침 새로고침을 누르면 같은 파일의 '이미 승인한 줄'이 무더기로 재검토로 뒤집힌다
- **지금(패치 후)**: 승인 판정 비교에서 **시급을 뺐다.** 시급만 바뀐 줄은 승인 상태와 승인 당시 금액을 그대로 유지한다. 승인한 줄까지 새 시급으로 다시 계산하려면 **그 파일을 승인대기로 되돌린 뒤 새로고침**한다(운영자의 명시적 결정). 새로고침 안내 문구도 이 규칙으로 바꿨다. **예외 하나**: 재승인 화면의 '시급 임의지정'은 의도적 경로이므로, 입력한 시급이 승인 당시 시급과 **다르면** 같은 행을 다시 승인할 수 있다(같으면 여전히 '이미 승인됨'). 시급을 비교에서 빼자 이 경로까지 막힌 것을 전체 시험이 잡아 고쳤다(`performance-approval-flow-service.ts`의 `isDeliberateRateChange`).
- **옛 결함(0.5.4 배포판)**: 승인 여부 판정이 지금 읽은 줄과 승인 당시 스냅샷을 통째로 비교하는데 비교 항목에 시급이 들어 있어, 과거 날짜로 시급을 고친 뒤 새로고침하면 대기함에 남은 파일의 승인 줄이 전부 재검토로 뒤집혔다.
- **근거**: `performance-approval-resolution-service.ts`의 `toComparableEntry`·`toSourceSignatureComparableEntry`(시급 없음) · 시험 "should not require reapproval when only the hourly rate changed"(서명 있는 줄·없는 줄 각각)

### T-3. [높음] ✅고침 '현재 시급' 표시는 날짜를 보지 않는다 — 미래 시급이 지금 시급처럼 보인다
- **지금(패치 후)**: 목록·상세 모두 **오늘 유효한 줄**을 보여준다. 아직 시작 안 한 줄은 "…부터 …원이 적용될 예정"으로 따로 표시하고, 오늘 유효한 줄이 없으면 금액·적용일·종료일 모두 `-`.
- **옛 결함(0.5.4 배포판)**: '종료일이 비어 있는 줄 중 최신'을 그냥 보여줘, 미래 시급이 현재 시급으로 보였다.
- **근거**: `wage-rate-timeline.ts`의 `findWageRateOnDate`·`findUpcomingWageRate` · `employee-storage-service.ts`의 `listStoredEmployees` 목록 쿼리(`effective_from <= 오늘 AND (effective_to IS NULL OR effective_to >= 오늘)`)
- **시험**: `wage-rate-timeline.test.ts`의 `findWageRateOnDate`·`findUpcomingWageRate` 묶음 · `employee-storage-service.test.ts`의 "keep today's wage as the current wage when a future rate is registered"

### T-4. [높음] ✅고침 '지난 날짜로 적용' 경고가 안 뜨는 소급 구간이 있다
- **지금(패치 후)**: 경고 기준이 **오늘**이다. 오늘보다 앞선 날짜면 무조건 확인창이 뜨고, 같은 적용일 줄이 있으면 사라질 금액까지 보여준다.
- **아직 남은 것**: 미래 날짜에는 경고가 없다(달력 상·하한도 없다). 오타로 먼 미래를 넣는 것은 여전히 안 막힌다.
- **옛 결함(0.5.4 배포판)**: 기준이 '지금 열려 있는 줄의 시작일'이라, 그 시작일보다 뒤이면서 오늘보다 앞선 날짜 — 가장 흔한 소급 — 에 경고가 없었다.

### T-5. [보통] ✅고침 적용일 칸이 미리 채워지고, 그 값은 오늘이 아닐 수 있다
- **지금(패치 후)**: 기본값은 **언제나 오늘**이다. 금액 칸에 지금 시급이 미리 들어가는 것은 그대로(편의).
- **옛 결함(0.5.4 배포판)**: `오늘`과 `현재 적용일 + 1` 중 늦은 쪽으로 채워, 그대로 두고 저장하면 의도한 날짜가 아니었다.
- **근거**: `WorkforceManagementScreen.tsx`의 `createInitialWageRateFormState`. 옛 헬퍼 `getNextWageEffectiveFrom`은 삭제됐다.

### T-6. [보통] ✅고침 '종료일(자동)' 미리보기가 소급·같은 날짜 입력에서 실제와 다르다
- **지금(패치 후)**: 저장 결과를 그대로 계산해 보여준다 — 새 줄의 구간, 앞줄이 끊기는 날, 같은 날짜면 덮어쓴다는 안내. 이력에 공백이 있어 앞줄이 새 적용일까지 이어지지 않으면 **끊긴다고 말하지 않는다**(저장 SQL과 같은 조건). **적용일을 걸치는 줄이 여럿이면 그 개수까지 말한다** — 저장은 전부 끊는데 단건 화면이 한 줄만 끝나는 것처럼 설명하던 것을 고쳤다(일괄 화면과 같은 규칙).
- **옛 결함(0.5.4 배포판)**: "고른 적용일의 전날"을 기계적으로 계산해, 과거 날짜에서 시작일보다 앞선 종료일이 표시됐다.
- **근거**: `wage-rate-timeline.ts`의 `buildWageSavePreview` ↔ `employee-history-service.ts`의 앞줄 끊기 UPDATE

### T-7. [높음] ✅고침 일괄 적용은 사번을 안 쓰고 '현재 배정 근무지 이름 + 사람 이름'으로만 찾는다
- **지금(패치 후)**: 모달에 **'사번 열'** 이 생겼고, **파일을 고르면 1행 머리글을 읽어 자동으로 채운다**('사번'·'사원번호' 등). 머리글에 사번이 없으면 비워 두고 **"사번 없이 찾으면 빠질 수 있다"고 경고**한다(자리로 넘겨짚지 않는다). 사번이 있으면 **사번으로 사람을 찾는다** — 근무지 이름이 달라도, 근무지를 옮겼어도, **활성 배정이 아예 없어도** 찾아낸다.
  - **사번과 이름이 서로 다르면 적용하지 않는다**('사번·이름 불일치'로 제외). 사번 오타가 엉뚱한 사람의 시급을 올리는 것을 막는 장치다.
  - 사번을 못 찾으면 '사번 없음'으로 제외하고, **제외 목록에 파일에 적힌 사번을 그대로 보여 준다**.
  - 파일의 근무지와 실제 배정 근무지가 다르면 적용은 하되 **"현재 배정 근무지는 ○○입니다"** 라고 알려 준다.
  - **정확히 같은 철자를 먼저 찾고**, 없을 때만 대소문자를 무시한다. 그 결과가 둘 이상이면 **아무것도 고르지 않고** 수동 확인으로 돌린다.
  - 이름 비교는 **공백만 다듬은 정확 비교**다(대소문자·이체자 허용 안 함). 느슨하게 하면 거부가 지급으로 바뀐다.
  - 배정이 없는 사람은 표에 **'미배정'** 으로 보여 준다 — 파일에 적힌 근무지를 실제 근무지처럼 보여 주지 않는다.
- **사번 열을 비워도 된다** — 비우면 예전처럼 근무지명+이름으로 찾으므로 기존 엑셀 양식이 그대로 동작한다.
- **운영자에게**: 엑셀에 사번 칸이 있다면 그 열 문자를 '사번 열'에 넣으세요. 누락이 거의 없어집니다.
- **근거**: `workforce-wage-bulk-update-service.ts`의 `buildEmployeeLookup`(`byEmployeeCode`·`bySiteAndName` 두 색인) · 시험 "should find a person by employee code when the site in the file is wrong" / "should refuse a row whose employee code and name disagree" / "should apply by employee code to a person with no current assignment"

### T-8. [높음] ✅고침 같은 근무지에 이름이 같은 퇴사자가 있으면 재직자까지 통째로 제외된다
동명이인 판정이 퇴사 판정보다 먼저 이뤄져 둘 다 '동일 인력 중복'이 된다.
- **근거**: `workforce-wage-bulk-update-service.ts:284-295`(중복 판정) · `:300-319`(퇴사 판정이 뒤)

### T-9. [높음] ✅고침 미리보기를 만든 뒤 적용일·열을 바꿔도 미리보기 표가 그대로 남는다
- **지금(패치 후)**: 미리보기도 적용 결과도 **자기가 만들어진 기준(파일·적용일·열)** 을 들고 다닌다. 지금 화면의 입력과 기준이 다르면 표가 그려지지 않고 [적용]도 잠긴다. 늦게 도착한 옛 응답이 표를 되살리지 못한다.
- **적용 중에는 파일·열·적용일·닫기가 모두 잠긴다**(Esc 포함). 저장이 도는 동안 입력을 바꿔 놓고 결과만 받아보는 상황 자체를 없앴다.
- **옛 결함(0.5.4 배포판)**: 표가 그대로 남아, [적용]이 다시 판정한 결과와 화면이 달랐다.
- **옛 결함(1차 패치)**: 보호가 **미리보기 응답에만** 걸려 있어, 적용 중에 파일이나 날짜를 바꾸면 A 파일 저장 결과가 **B 파일 이름·B 적용일 옆에** 표시됐다.

### T-10. [보통] ✅고침 일괄 적용에는 '지난 날짜' 확인창이 아예 없다
- **지금(패치 후)**: 일괄 적용에도 지난 날짜 확인창이 뜬다(대상 인원수와 새로고침 안내 포함).
- **옛 결함(0.5.4 배포판)**: 확인창이 없어 수십 명분이 확인 없이 즉시 저장됐다.

### T-11. [높음] ✅고침(안내) 소급 적용은 '그 뒤에 이미 등록된 시급 줄'을 덮지 않는다
- **지금(패치 후)**: 동작은 R-7대로 그대로다(뒤 줄은 보존). 대신 일괄 미리보기의 '적용 가능' 행에 **"이미 8/1자 시급이 등록되어 있어 새 시급은 7/31까지만 적용됩니다. 지금 시급은 바뀌지 않습니다"** 를 사람별로 붙인다(뒤 줄이 이미 시작된 경우). 아직 시작 안 한 뒤 줄은 '새 시급 종료일' 칸이 이미 말해 준다.
- **옛 결함(0.5.4 배포판)**: 7월 1일자로 일괄 인상해도 8월 1일자 줄이 있는 사람은 현재 시급이 안 바뀌는데, 화면 어디에도 안내가 없어 빠진 사람을 알아채기 어려웠다.
- **운영자 대응**: 일괄 인상 후 **적용된 인원수**와 행별 안내를 확인한다.
- **근거**: `wage-bulk-preview-basis.ts`의 `describeWageBulkRow`(`laterRateNote`) · 시험 "says so when a later line already in force keeps the current wage unchanged"

### T-12. [높음] ✅고침 입사일은 등록할 때 한 번만 정할 수 있고 뒤에는 못 고친다
- **지금(패치 후)**: 인력 상세의 기본정보에서 **입사일을 고칠 수 있다.** 검사는 화면과 저장(main)이 **같은 함수**를 쓴다(`employee-dates.ts`): 실제 달력 날짜인지(2/30·99/99 거절), 1990-01-01~오늘, 퇴사일은 실제 날짜이고 입사일보다 빠를 수 없음. 저장(main)은 화면과 같은 필수 규칙도 강제한다 — **입사일은 반드시 있어야 하고, 퇴사 처리일은 퇴사 상태일 때만·그때는 반드시** 있어야 한다(빈칸·공백은 "없음"으로 보고 `""`를 저장하지 않는다, R10 #1). 입사일이 첫 시급 시작일과 다르면 **양방향** 경고 — 늦으면 "입사 전 시급 줄이 남는다", **빠르면 "입사일~첫 시급 전날에 시급이 없어 그 기간 승인이 막힌다"**(위험한 쪽). **인력 기본정보 변경 → 재분석 규칙**: 신규 등록, 이름·사번·입사일·퇴사일 변경, 근무지 배정 저장·해지, **근무지 이름 변경·근무지 삭제**는 파서의 후보 해석을 바꾸므로 **같은 트랜잭션 안에서 재분석 표시(토큰)** 를 남기고, 다음 실적 관리 조회가 승인대기 파일을 다시 읽는다. 표시는 조회가 먼저 읽어 두고(peek) 다시 읽기가 끝난 뒤 **읽었던 토큰으로만** 지운다 — 다시 읽기가 실패하면 표시가 남아 다음 조회가 다시 시도하고, 다시 읽는 도중 생긴 새 표시는 살아남는다(R10 #2·#5). **연·월을 고른 조회는 그 달 파일만 다시 읽으므로 표시를 지우지 않고 "이 토큰으로 그 달은 읽었다"만 기록한다** — 같은 달을 두 번 읽지 않고, 다른 달은 그 달을 열 때 읽고, 새 변경(새 토큰)이 생기면 기록이 처음부터 다시 시작되며, **전체 기간 조회만** 표시를 지운다(수당 화면의 승인완료 조회도 같은 규칙, R11 자체검증). 연락처·직급·상태만 바꾸면 남기지 않고, **시급 변경은 이 표시와 무관**(T-1: 수동 새로고침). 완료창에 그 안내를 적는다. **근무 설정의 조 근무유형(Pool/주간고정/교대)이 실제로 바뀌거나 설정 버전이 비활성화되면** 같은 방식의 표시(종류 '조 근무유형')를 남긴다 — 대체수당 지급 여부가 그 유형으로 정해지기 때문. 이름·시간·정원만 바뀐 저장은 남기지 않는다(2026-09-05, `shift-pattern-storage-service.ts`의 `haveTeamWorkTypesChanged`, 시험 "leaves the team work-type reparse marker only when a team's work type actually changes"). 근무유형이 바뀐 저장은 마법사의 다음 완료창(1단계 적용 완료·2단계 저장 완료·배정 저장 완료 중 먼저 뜨는 것)에 "조 근무유형이 바뀌었습니다. 실적 관리에 들어가면 승인대기 실적을 새 근무유형으로 다시 읽습니다"를 한 번 보여 준다. **월간 근무표를 저장(처음 저장·같은 달 재생성)하면** 같은 방식의 표시(종류 '근무표')를 **저장 트랜잭션 안에서** 남긴다 — 파서는 휴일 칸 시간·자기 정규근무 여부·적용 설정 버전을 근무표 저장본에서 읽으므로. 근무표 배포 완료창에 안내 한 줄. 저장이 실패(사번 없음)하면 되돌려져 표시도 안 남는다(`monthly-schedule-storage-service.test.ts` "leaves the monthly-schedule reparse marker on a first save and again on a regeneration"). **비용**: 배정을 많이 고친 뒤 그 달을 처음 열면 그 달 승인대기 파일을 전부 한 번 다시 읽어 시간이 걸린다(진행 표시가 뜬다).
- **옛 결함(0.5.4 배포판)**: 입력칸이 신규 등록 창에만 있고, 달력에 상·하한이 없어 미래·오타 날짜가 그대로 들어갔다.
- **알아 둘 것 — 입·퇴사일이 파싱 매칭에 미치는 영향은 상황별로 다르다**(R10 #3, 아래 표). 근무표 편성(`monthly-schedule-draft`)에는 언제나 관문이다.

| 파싱 입력 상황 | 입·퇴사일의 영향 |
|---|---|
| 근무표에서 **사번 힌트**를 찾은 행(법정휴일 행) | 날짜 필터를 **우회**한다. 그 사번으로 그대로 매칭 |
| 이름 후보가 **1명** | 기간 밖이어도 전체 후보 폴백으로 **그대로 매칭**(누구인지 보이도록). 단 T-22 오류가 붙어 **승인은 막힌다** |
| 동명이인 중 **가용(기간 안) 후보가 1명 이상** | 입·퇴사일이 **후보를 줄인다** — 다른 사번·다른 시급으로 바뀔 수 있다 |
| 동명이인 **모두 기간 밖** | 전체 후보로 폴백한 뒤 근무지로 좁히거나 동명이인 모호성 오류 |

- **근거**: `employee-dates.test.ts` · `employee-storage-service.ts`의 `saveStoredEmployee`(검사·재분석 표시) · `employee-storage-service.test.ts` "requires a hire date, and a retire date exactly when the person is retired" · "leaves the employee master reparse marker for a new person and for a changed key field only" · `employee-history-service.test.ts` "leaves the employee master reparse marker when an assignment is saved or closed" · `performance-management-service.test.ts` "reads the pending files again once after a hire date changes…" · "finds a person registered after the file was parsed, on the next plain overview" · "keeps the reparse marker when the re-read fails…" · `schedule-return-performance-parser.test.ts` "narrows same-name candidates by their employment period…" · `describeHireDateAgainstWages` 시험 4건

### T-13. [보통] ✅고침(경고) 저장 버튼을 한 번 더 누르면 중복 줄이 생기고 막아주지 않는다
- **지금(패치 후)**: 같은 적용일 재저장에 확인창이 뜬다(사라질 금액을 보여준다). 저장 버튼은 원래 저장 중 잠긴다.
- **옛 결함(0.5.4 배포판)**: 저장 후 입력칸이 다시 채워져(T-5), 한 번 더 누르면 조용히 덮어썼다. 실제 개발 PC 자료에 **6초 간격 중복 저장 흔적**이 있다(2026-09-04).

### T-14. [보통] 시급 줄을 '종료'하고 뒤를 잇는 줄을 안 만들면 그 뒤 근무는 시급을 못 찾는다
종료 기능은 `effective_to`만 채우고 다음 줄을 만들지 않는다. 그 뒤 기간은 시급 공백이 되어 R-11로 승인이 막힌다.
- **근거**: `employee-history-service.ts:520-543`

### T-15. [보통] 시급 이력보다 앞선 기간의 근무는 승인이 막힌다 — 이관 데이터에서 특히 위험
R-20 때문에 이관된 인력의 시급 시작일이 실제 근무 시작보다 늦은 경우가 많다. **그 이전 달을 다시 불러오거나 새로 계산하면** 시급을 못 찾아 막힌다. 이미 승인이 끝난 기존 기록은 금액이 보존되므로 안전하다.
- **대응**: 옛 달을 건드리기 전에 `diagnose-wage-history.cjs`로 공백 구간을 먼저 확인한다.

### T-16. [보통] ✅고침(안내) 재승인 화면의 '시급 임의지정'은 근무일이 아니라 '오늘' 날짜로 이력에 저장된다
- **지금(패치 후)**: 동작은 설계대로 그대로다("과거 기간을 고치는 게 아니라 오늘부터 시급을 바꾼다"). 확인창이 그 뜻을 **그대로 말하고**, 과거 기간을 바로잡는 길(인력 관리 > 시급 변경)을 안내한다. 오늘 시작하는 줄이 이미 있으면 **덮어쓰기 확인창**이 뜬다(시급 화면과 같은 규칙).
- **옛 결함(0.5.4 배포판)**: 확인창이 날짜만 말하고 뜻은 말하지 않았고, 같은 날짜 경고가 이 경로엔 없었다.
- **근거**: `PerformanceManagementScreen.tsx`의 `updateCurrentEmployeeWageRate`(같은 날짜 확인)·`handleConfirmManualHourlyRate`(문구)

### T-17. [보통] ✅고침(안내) 승인대기로 되돌려도 시급은 다시 붙지 않는다
- **지금(패치 후)**: 동작은 그대로다(되돌리기는 승인 기록과 수당 계산만 지운다). 되돌리기 **확인창과 완료창**이 "시급은 자동으로 다시 붙지 않는다, 시급을 바꿨다면 새로고침(↻)으로 다시 읽은 뒤 승인하라"고 말한다. 품의 승인까지 간 건은 되돌리기 자체가 막힌다.
- **옛 결함(0.5.4 배포판)**: 아무 안내가 없어, 시급을 고치려고 되돌린 뒤 옛 시급 그대로 재승인할 수 있었다.
- **근거**: `PerformanceManagementScreen.tsx`의 `handleReturnApprovedToPending` · `performance-approval-flow-service.ts`의 `returnApprovedPerformanceFileToPending`

### T-18. [보통] ✅고침 일괄 적용의 '퇴사자 제외'는 적용일이 아니라 '지금 상태'로 판정한다
- **지금(패치 후)**: **퇴사일 기준**으로 판정한다. 퇴사일이 적용일과 **같거나 앞서면 제외**, 적용일 다음날 이후까지 근무했으면 소급 인상이 그 사람에게도 반영된다. 퇴사일이 없는데 상태만 '퇴사'면 안전하게 제외.
  - 이 프로그램에서 **퇴사 처리일은 '마지막 근무일'이 아니라 '더는 근무하지 않는 첫날'** 이다(근무표 초안·실적 파서 모두 그 날짜의 근무를 거부한다). 그래서 퇴사일이 적용일과 같으면 인상분이 닿을 근무일이 하루도 없어 제외한다.
  ⚠️ **금액에 닿는 변경이다** — 이미 퇴사한 사람에게도 그가 근무한 기간의 소급 인상이 적용된다. 문서가 원래 약속하던 동작이지만, 지급 대상이 늘어난다.
  ✅ **사용자 승인(2026-09-04)**: 이대로 간다. 근거 = 실제로 근무한 날에 대한 인상분이므로 지급이 맞고, 화면 안내가 원래 약속하던 동작이며, 승인된 실적을 덮어쓰지 않는다.
- **아직 남은 것**: 삭제된 근무지에 배정된 인력·휴직자는 여전히 안 걸러진다(화면 목록에서는 감춰지는데 일괄 대상에는 포함).
- **근거**: `workforce-wage-bulk-update-service.ts`의 `hadLeftBefore` · 시험 "should include a leaver whose last day is on or after the effective date"

### T-18-2. [보통] ✅고침 미리보기가 '새 시급이 언제까지인지'를 안 보여 준다
'종료일(자동)' 한 칸이 **앞줄이 끊기는 날**을 보여 주고 있었다. 새로 넣는 시급이 언제까지인지는 화면 어디에도 없었다.
- **지금(패치 후)**: **'이전 시급 종료일'** 과 **'새 시급 종료일'** 을 나눠 보여 준다. 뒤에 시급이 없으면 **'계속'**. 같은 적용일 줄을 덮어쓰는 행은 **'같은 적용일 기존 이력 덮어쓰기'** 라고 딱지를 붙인다.
- **끊길 줄이 실제로 있을 때만** 이전 종료일을 적는다. 이력에 공백이 있어 아무 줄도 안 끊기면 `-`(T-6과 같은 규칙을 일괄 화면에도 적용). 여러 줄이 끊기면 **"N개 이력 종료"** 까지 적는다.
- **근거**: `wage-bulk-preview-basis.ts`의 `describeWageBulkRow`

### T-18-1. [높음] ✅고침 미리보기 뒤 **같은 파일을 고쳐 저장하면**, 아무도 못 본 시급이 적용된다
적용은 화면이 보낸 표를 믿지 않고 파일과 인력 정보를 **다시 읽는다**(그 자체는 옳다). 그래서 미리보기를 만든 뒤 엑셀에서 같은 파일을 고쳐 저장하면, 화면에는 옛 표가 그대로인데 저장은 새 내용대로 됐다.
- **지금(패치 후)**: 미리보기를 만들 때 **그 판정 내용의 지문**(파일 내용·적용일·대상자·금액)을 함께 만들어 두고, 적용은 그 지문과 일치할 때만 실행된다. 파일이 바뀌었거나 그 사이 인력·시급이 바뀌었으면 **적용을 거부하고** "미리보기를 다시 만들어 확인한 뒤 적용해 주세요"라고 알린다. 하나도 저장되지 않는다.
- **지문에 들어가는 것**: 파일명·시트명·적용일·판정된 행 전체(대상자·금액·상태)에 더해 **저장 계획 전체**(`savePlan`) — 넣기인지 덮어쓰기인지, **덮어쓸 줄의 정확한 id**, 새 줄의 종료일, 그리고 **끊길 앞줄들의 id와 지금 종료일**. 개수나 참/거짓이 아니라 **대상 자체**를 못 박는다(겹친 과거 줄이 몰래 늘어도 잡힌다). 종료일은 **적용 시점의 다음 미래 시급**이 정하므로, 미리보기 뒤 미래 시급이 하나 추가되면 화면이 약속한 기간과 실제 저장 기간이 달라진다 — 그래서 지문에 넣었다.
- **거부되면 화면이 미리보기를 지운다** — 다시 만들기 전에는 [적용]이 잠긴다. 전용 오류 코드 `WORKFORCE_WAGE_BULK_PREVIEW_STALE`로 판단하며, 일반 저장 실패는 미리보기를 지우지 않는다.
- **근거**: `workforce-wage-bulk-update-service.ts`의 `buildPreviewFingerprint` · 시험 "should refuse to apply when the same file changed after the preview" / "…when a future wage line appeared after the preview" / "…when the matched person left after the preview"

### T-19. [보통] ✅고침(표시) 시급 변경 이력 화면은 글줄 목록이라 겹침·구멍이 안 보이고 고칠 수도 없다
- **지금(패치 후)**: 이력 목록 위에 **"겹침 N건 · 공백 N건"** 요약이 뜨고, 각 줄 아래에 "앞 줄(시작~끝)과 겹침(겹치는 날은 이 줄로 계산)" · "같은 시작일 줄 N개(가장 나중에 만든 줄만 계산에 쓰임)" · "사이에 시급 없는 기간 M/D~M/D" · "끝난 뒤 이어지는 줄 없음"을 붙인다. 판정은 **지금까지 덮인 가장 먼 끝**을 끌고 가는 스윕이다 — 열린 줄은 뒤를 전부 덮으므로 "1/1~계속, 2/1~2/28, 4/1~계속"에서 3월은 공백이 아니고 4월 줄은 겹침이다(직전 줄만 보던 첫 구현은 반대로 말했다, R9 #4). 안내 하나하나는 종류 4가지(같은 시작일 · 앞 구간 겹침 · 사이 공백 · 뒤 공백) 중 하나이고 **한 줄에 한 종류는 한 번**만 붙으므로 `종류:줄id`가 고유 id다 — 앞 줄이 열려 있는데 같은 시작일 줄이 둘이면 이긴 줄에 "같은 시작일"과 "앞 구간 겹침" 두 안내가 함께 붙는다(R10 #4). **요약 건수는 문장 수가 아니라 고칠 원인 수**: 같은 시작일 줄 셋은 겹침 1건(날짜 하나만 다시 저장하면 됨), 위 사례는 겹침 2건. **수정·삭제는 넣지 않았다**(R-13).
- **옛 결함(0.5.4 배포판)**: 글줄 목록뿐이라 겹침·구멍이 보이지 않았다.
- **근거**: `wage-rate-timeline.ts`의 `describeWageHistoryIssues`·`summarizeWageHistoryIssues` · 시험 9건(중첩 2·같은 시작일 3개·후행 공백·복합 겹침 id·원인 수 집계 포함)

### T-20. [낮음] ✅고침 인력 저장 경로의 시급 쓰기는 시급 화면과 규칙이 다르다
- **지금(패치 후)**: 신규 등록의 시급 줄도 **시급 화면과 같은 함수**(`saveStoredEmployeeWageRate`)로 쓴다 — 입사일부터 시작, 그 날짜를 걸치는 줄은 전날로 끊음. 인력·첫 배정·첫 시급은 **한 트랜잭션**이라 마지막 쓰기가 실패하면 앞의 둘도 남지 않고, 같은 사번으로 바로 다시 등록할 수 있다(R9 #6).
- **옛 결함(0.5.4 배포판)**: 열린 줄을 **오늘 날짜로** 닫고 새 줄을 **입사일부터** 넣어 기간이 겹칠 수 있었다. 트랜잭션이 없어 시급 쓰기 실패 시 인력만 반쯤 남고 재등록이 사번 중복으로 막힐 수 있었다.
- **근거**: `employee-storage-service.ts`의 `saveStoredEmployee`(BEGIN/COMMIT) · 시험 "registers the person, the assignment and the first wage line together or not at all"(트리거로 시급 INSERT를 실패시킴)

### T-21. [낮음] ✅고침 UTC 기준 '오늘'이 아직 두 곳에 남아 있다
- **지금(패치 후)**: 품의서 인쇄일(Excel 두 군데·**PDF 한 군데**)과 요율 저장 검사가 모두 **로컬 날짜**(`createTodayDateInputValue`)를 쓴다. PDF는 내보내기가 정한 인쇄일을 받아 같은 작업의 문서번호와 날짜가 같다. 신규 등록의 배정·시급 시작 기본값도 같이 고쳤다.
- **옛 결함(0.5.4 배포판)**: `toISOString()`이 UTC라 한국시간 자정~오전 9시 사이에는 하루 전 날짜가 찍혔다. 첫 수정은 Excel만 고치고 PDF를 놓쳤다(R9 #5).
- **근거**: `local-date.test.ts` · `resolvePdfPrintedDate` 시험(로컬 00:30 고정) · `allowance-document-export-service.ts`·`allowance-document-pdf-service.ts`·`operations-storage-service.ts`에 `toISOString().slice(0, 10)` 없음

### T-22. [보통] ✅고침(차단) 입사일 이전·퇴사 처리일 이후 근무 행이 그대로 승인된다
- **지금(패치 후, 2026-09-05 사용자 결정)**: 매칭된 사람의 **입사일 이전** 또는 **퇴사 처리일 당일·이후** 근무 행에 **오류**가 붙어 **승인이 막힌다** — "M/D 근무는 홍길동(사번)의 입사일(YYYY-MM-DD) 이전입니다. 고용 기간 밖 근무는 승인할 수 없습니다. 입사일이 잘못됐다면 인력 관리에서 고치세요. 고치면 이 파일을 다시 읽습니다." 행은 그대로 매칭돼 누구인지 보이고, 승인 버튼은 "오류 알림이 남아 있어 승인할 수 없습니다"로 거절한다. **시급 임의지정으로도 못 넘어간다**(임의지정은 문구에 '시급'이 든 알림만 걷어내는데 이 문구에는 '시급'이 없다). 시급이 없어 막힌 행에도 같은 오류가 함께 붙는다(같은 오타의 두 증상). 판정은 사람 자신의 입·퇴사일만 보고, 배정 기간에 따른 가용 판정은 별개다.
- **푸는 길**: 인력 관리에서 입사일·퇴사 처리일을 고치면 재분석 표시가 남아(T-12) 다음 실적 관리 조회가 그 파일을 다시 읽고 오류가 사라진다. **퇴사 처리일은 첫 비근무일**이므로 그날 근무를 지급하려면 처리일을 하루 뒤로 옮긴다. 삭제(보관)된 퇴사자의 처리일 이후 근무는 고칠 사람이 없어 승인 불가 — 그 사람을 다시 등록해야 한다.
- **옛 동작(R11~R14)**: 경고만 붙고 승인은 됐다(퇴사·삭제 인력의 늦은 제출을 지급하려고). 사용자가 "고용 기간 밖 근무는 승인되면 안 된다"고 정해 차단으로 바꿨다.
- **알아 둘 것**: 승인 비교는 원천 서명이 있는 행(법정휴일·대체·연장)에서 알림을 보지 않으므로 이 오류가 새로 붙어도 승인 행이 재승인으로 돌아가지 않는다. 다만 **서명 없는 옛 승인분**(서명 도입 전 승인)은 알림까지 비교하므로, 승인완료 범위를 수동 새로고침하면 그런 행은 재승인 대상이 될 수 있다.
- **근거**: `schedule-return-performance-parser.ts`의 `describeEmploymentPeriodMismatch`·`narrowEmployeeCandidates` · `performance-approval-flow-service.ts`의 `validateApprovalEntry`(오류 알림 = 승인 거절) · 시험 "flags a row outside the only match's employment period as an approval-blocking error, and still matches it" · `performance-approval-flow-service.test.ts` "refuses to approve a row worked before the hire date, with or without a manual hourly rate"(입사일 변경→다시 읽기→승인 거절까지 한 줄로) · "should parse an archived retired worker..."(퇴사·삭제 인력의 **처리일 이전** 근무는 지급)

### T-23. [보통] ✅고침 입사일은 배정·시급·근무표의 바닥이다 — 달 중간 전보자가 1일부터 편성되던 것 포함
- **규칙(2026-09-05 사용자 결정)**: ① **배정 시작일은 입사일보다 빠를 수 없다**(저장이 거절: "배정 시작일은 입사일(…)보다 빠를 수 없습니다"; 근무지 마법사 2단계는 드롭하는 순간 같은 문구로 알려 준다). ② **시급 적용일은 입사일보다 빠를 수 없다**(저장이 거절: "시급 적용일은 입사일(…)보다 빠를 수 없습니다"; 인력 상세의 시급 적용일 달력은 입사일 이전 날짜를 회색으로 막는다). 등록 창의 첫 시급은 입사일에 시작하므로(R-2) 그대로 통과. ③ **근무표 편성 시작일 = 입사일과 현재 배정 시작일 중 늦은 날**(근무표 편성 `monthly-schedule-draft`·근무표 화면 둘 다 `getEmployeeScheduleStartDate`). ①이 지켜지는 자료에서는 곧 배정 시작일이라 **달 중간에 근무지를 옮긴 사람은 새 근무지에 옮긴 날부터** 편성된다(옛 근무지는 배정 종료일부터 빠지므로 겹치지 않는다). 규칙 이전에 저장된 "입사일보다 빠른 배정"은 입사일이 이긴다(입사 전에 일하는 사람은 없다). 입사일이 없는 옛 자료는 배정 시작일만 본다(R-19).
- **일괄 적용**: 적용일이 입사일보다 빠른 사람은 미리보기에서 **"입사 전 제외"** 로 따로 빠진다(파일 전체를 실패시키지 않는다). 그 사람은 인력 상세에서 입사일 이후 날짜로 따로 넣는다. 적용일 = 입사일이면 적용된다.
- **입사일을 배정 시작일보다 뒤로 고치면**: 저장은 막지 않는다(배정 시작일을 고칠 화면이 없어 막으면 갇힌다). 대신 기본 정보 칸 아래 "입사일이 현재 배정 시작일(…)보다 늦습니다. 근무표 편성과 실적 승인은 입사일부터입니다"를 보여 준다.
- **파서는 그대로**: 실적 파서의 가용 판정(`isEmployeeAvailableOnDate`)은 "입사일, 없으면 현재 배정 시작일"을 유지한다. 파서는 행마다 **배정 이력 전체**로 근무지를 좁히므로(`hasAssignmentAtSiteOnDate`) 현재 배정 시작일을 바닥으로 쓰면 전보 이전 달의 옛 근무지 행이 비가용이 된다. 고용 기간 판정은 T-22.
- **옛 동작(0.5.4~R14)**: 세 곳이 "입사일, 없을 때만 배정 시작일"이라 입사일 3/1·배정 3/15인 사람이 3/1부터 편성됐다. 배정·시급이 입사일보다 빠른 저장을 막는 곳이 없었다.
- **근거**: `shared/domain/employee-dates.ts`(`describeAssignmentStartAgainstHireDate`·`describeWageEffectiveFromAgainstHireDate`·`getEmployeeScheduleStartDate`, 시험 5건) · `employee-history-service.ts`의 `saveStoredEmployeeAssignment`·`saveStoredEmployeeWageRate`(시험 "refuses a wage line that would apply before the hire date…" · "refuses an assignment that would start before the hire date…") · `monthly-schedule-draft.test.ts` "should start the draft on the later of the hire date and the assignment start" · `workforce-wage-bulk-update-service.test.ts` "sets aside a person hired after the effective date…" · `site-management-step-two-actions.ts`의 `handleAssignEmployee`

---

## 3. 실측 (개발 PC, 2026-09-04) — 데이터는 변하므로 날짜와 함께 읽을 것

`node artifacts/scripts/diagnose-wage-history.cjs` 로 언제든 다시 뽑을 수 있다.

| 항목 | 결과 |
|---|---|
| 인력 / 시급 이력 | 86명 / 86줄 |
| 구간 겹침 · 구멍 · 0원 · 열린 줄 중복 | **모두 0건** (자동 정리 정상 동작) |
| 엑셀로 올린 실적의 시급 커버율 | **33 / 33건 정상** |
| 시급 이력이 0줄인 재직자 | 2명 (Pool 1 · BP 1 — 구조상 예외일 가능성 높으나 확인 권장) |
| 시급 이력 출처 | 83줄이 2026-05-21 Access 일괄 이관, 시작일 70건이 **2024-11-04** 하루에 집중 |
| 시급이 안 붙은 옛 실적 283건 | 전부 Access 이관분 + **이미 승인 완료** → 지금 막히는 것 없음 |
| 다시 승인하면 실제로 막히는 건 | **1건** (2024-08-15 SK증권 '홍길동1', 시급 NULL) |
| 소수점 시급 | 12건 — 화면·서류·계산 모두 소수를 그대로 쓰므로 **금액 오류 없음** |
| 시급 일괄 적용 사용 이력 | 0회 (활동기록 328건 중) |

**핵심 해석**: 시급 장부 자체는 건전하다. 위험은 **2024-11-04 이전 기간을 다시 불러올 때**(T-15)와 **시급 변경 후 반영 절차**(T-1, T-2)에 있다.

---

## 4. 아직 시험이 없는 것 (다음에 손대면 여기부터 못 박을 것)

- ✅ **시급 → 실적 금액 반영** (T-1) — 파서 시험 "stamps the wage in force on the work date, and only a new parse picks up a wage saved later": 근무일 당일 시작 줄은 적용(포함 경계), 다음날 시작 줄은 미적용, 이미 읽은 행은 새 시급을 저장해도 안 움직임.
- ✅ **시급 변경 → 재승인 판정 뒤집힘** (T-2) — 서명 있는 줄·없는 줄 각각 "should not require reapproval when only the hourly rate changed".
- ✅ Access 이관의 시급 1줄 압축 로직 (R-20) — `buildActiveWageMap` 시험: 적용유무 줄 중 시작일 최신 1줄만, 미적용·0원 제외, 날짜 없으면 이관 연도 1월 1일.
- ✅ 시급 줄 종료 후 공백 구간 (T-14) — `describeWageHistoryIssues` 시험 "마지막 줄이 과거에 끝나고 뒤가 없으면 그것도 공백이다"(표시 기준. 종료 기능 자체는 화면에 없음).
- ✅ 입사일과 시급 시작일의 정합성 — `describeHireDateAgainstWages` 시험 3건(경고 기준. 저장을 막지는 않음).

조사 시점(2026-09-04, 패치 전)의 시험은 `employee-history-service.test.ts` · `employee-storage-service.test.ts`(전부 신규 등록) · `workforce-wage-bulk-update-service.test.ts` · `excel-import-gap-campaign.test.ts` · `DateField.test.tsx` 뿐이었고, 사실상 **"한 사람의 시급 줄을 넣을 때 기간이 겹치지 않는가"** 하나만 보증했다. 그 뒤 아래 5절의 패치로 `wage-rate-timeline.test.ts`·`wage-bulk-preview-basis.test.ts`가 생겼지만, **위 목록의 공백 자체는 그대로다.** 시험 개수는 계속 바뀌므로 여기 적지 않는다 — `npm run test` 결과를 보라.

---

## 5. 1차 패치 기록 (2026-09-04, 브랜치 `fix/wage-ui-batch-1` · **미게시** · 아래 수치는 그때의 역사값)

화면·표시만 손댄 8건. 계산식과 승인 판정은 건드리지 않았으므로 **재승인 위험 0**이다.

| 함정 | 고친 내용 |
|---|---|
| T-1 | 시급 저장·일괄 적용의 확인창과 완료 안내를 사실대로 바꾸고 **"실적 관리 화면에서 새로고침(↻)으로 다시 읽어야 반영된다"** 를 넣었다. 자동 재계산 자체는 아직 없다(2차 과제). |
| T-3 | 목록·상세의 '현재 시급'을 **오늘 유효한 줄**로 바꿨다(뒷단 조회 + 화면 판정). 미래 적용분은 "○○부터 ○○원이 적용될 예정" 으로 따로 보여 준다. |
| T-4 | 소급 경고 기준을 **오늘**로 바꿨다. 같은 적용일이면 기존 금액을 함께 보여주며 "이전 금액은 남지 않습니다"라고 알린다. |
| T-5 | 적용일 기본값에서 **'현재 적용일 + 1' 자동 채움을 없애고 언제나 오늘**로 했다. |
| T-6 | '종료일(자동)' 자리에 저장 결과를 그대로 계산해 보여 준다(새 줄 구간 + 앞줄이 끊기는 날, 같은 날짜면 덮어쓴다는 안내). |
| T-9 | 적용일이나 열 글자를 바꾸면 **미리보기 표를 지운다.** |
| T-10 | 일괄 적용에도 **지난 날짜 확인창**을 붙였다(대상 인원수 함께 표시). |
| T-13 | 같은 적용일 재저장에 확인창이 뜬다(저장 버튼은 원래 저장 중 잠긴다). |

**새로 생긴 것**: `src/renderer/screens/workforce/wage-rate-timeline.ts` — 시급 구간 판정(오늘 유효한 줄·예정 줄·저장 결과 미리보기)을 화면에서 떼어낸 모듈. 시험 13개.
**시험 보강**: `wage-rate-timeline.test.ts`(13) + `employee-storage-service.test.ts`에 '현재 시급 = 오늘 기준' 2건. 되돌리면 6건이 실패하는 것까지 확인했다.
**검증**: 866 테스트 통과 · typecheck · lint 0 error · `npm run validate:map` 통과.

### 5-1. Codex 적대검증에서 더 잡힌 것 (같은 브랜치, R1~R9 아홉 차례)

첫 패치는 통과하지 못했다. 여섯 번의 검증에서 **30건**이 더 나왔고 전부 실재였다 — 그중 스물셋은 그 패치들이 새로 만든 결함이었다. **되풀이되는 유형은 둘**: ①늦게 도착한 비동기 응답을 현재 화면에 묶지 않는 것(미리보기 → 적용 → 머리글 읽기, 세 번 반복) ②안전장치를 만들어 놓고 그 결과가 실제로 흐르는지 확인하지 않는 것. **가장 위험했던 것은 R5 #1** — 내가 "사번 오타를 막는 안전장치"라고 쓴 코드가, 대소문자만 다른 사번이 함께 있으면 **오히려 엉뚱한 사람의 시급을 조용히 바꾸는 경로**였다.

| 무엇 | 어떻게 고쳤나 |
|---|---|
| 일괄 미리보기 응답이 늦게 도착해 **낡은 표가 되살아남**(적용일·열 변경, **파일 교체**, 모달 재진입 전부) | 미리보기가 **자기가 만들어진 기준(파일·적용일·열)** 을 들고 다니게 하고, 기준이 다르면 화면에 보이지도 적용되지도 않게 했다(`wage-bulk-preview-basis.ts`). 무효화를 "잊지 않기"에 기대지 않는 구조 |
| 같은 적용일 줄이 둘일 때 **저장은 옛 줄, 화면·계산은 새 줄** | 저장이 읽기와 같은 줄(생성 늦은 쪽)을 고르게 함 |
| 같은 **미래** 적용일이 둘일 때 '예정' 표시만 옛 줄을 고름 | `findUpcomingWageRate`의 동률 규칙을 읽기와 맞춤 |
| 이력에 공백이 있으면 앞줄이 안 끊기는데 **끊긴다고 예고** | 미리보기 조건을 저장 SQL과 동일하게 |
| 오늘 유효한 시급이 없는데 **종료일만 '계속'** | `-`로 |
| 새로고침 안내가 **T-2 위험을 안 말함** | 문구에 "부분 승인 파일은 이미 승인한 줄도 재검토로 되돌아간다"를 넣음 |
| 자정에 예정 시급이 활성화되면 **입력 중이던 폼이 초기화** | 불필요한 의존성 제거 |
| 문서가 ✅만 붙이고 옛 결함을 현재형으로 서술 / 줄번호 근거가 이미 낡음 | 3단(지금/남은 것/옛 결함) 구조 + 근거를 **함수·시험 이름**으로 |
| 새 코드 주석이 한국어(레포 규칙 위반) | 이 브랜치 주석은 영어로. 기존 755줄은 별건 |

**R3에서 더 나온 3건**

| 무엇 | 어떻게 고쳤나 |
|---|---|
| 보호가 **미리보기에만** 걸려 있어, 적용 중 입력을 바꾸면 A 저장 결과가 B 파일·B 날짜 옆에 표시됨 | 적용 결과도 기준을 들고 다니게 하고, **무엇을 화면에 보일지 판정 자체를 순수 함수로 뗐다**(`selectWageBulkView`). 적용 중에는 입력·닫기도 잠근다 |
| 미리보기 뒤 **같은 경로의 파일 내용이 바뀌면** 안 본 내용이 적용됨 | 미리보기 판정의 지문을 만들고, 적용은 그 지문과 맞을 때만 실행(T-18-1) |
| 퇴사일 **동률 경계**가 근무표·실적 판정과 반대 | `<=` 로 맞추고 동률 시험 추가(T-18) |

**R6에서 더 나온 5건**

| 무엇 | 어떻게 고쳤나 |
|---|---|
| 머리글 읽기 결과가 **현재 파일에 묶여 있지 않음**(A→B 연속 선택 시 A 응답이 B를 덮음, 수동 입력도 덮음) | 매핑 세대 카운터 도입, `resolveWageBulkColumnSuggestion`이 세대가 다르면 버림 |
| 일부 머리글 미탐지 시 **직전 파일의 열을 물려받음** | 새 파일은 **기본값(B/C/D)** 에서 시작. 못 찾은 항목을 화면에 나열 |
| 이력 공백 구간에서 **끊기지도 않을 종료일을 표시**(T-6의 일괄판 재발) | `truncatedRates`가 비면 `-`, 여럿이면 개수까지 |
| 같은 머리글이 두 열이면 **조용히 첫 열 선택** | 자동 선택 안 하고 화면에서 알림(`ambiguousFields`) |
| 규칙 문서 R-16이 "머리글 자동 인식 없음"이라 **T-7과 모순** | R-16·상세 가이드 전부 갱신 |

**R5에서 더 나온 7건**

| 무엇 | 어떻게 고쳤나 |
|---|---|
| 🔴 대소문자만 다른 사번 + 퇴사자 조합에서 **엉뚱한 재직자 시급이 바뀜** | 사번 경로를 **한 명으로 좁힌 뒤에** 이름·퇴사를 본다. 정확 철자 우선, 대소문자 충돌은 수동 확인 |
| 지문이 **끊길 앞줄 전체**를 못 박지 못함 | `savePlan`에 끊길 줄들의 id·현재 종료일까지 포함 |
| 같은 시작일 덮어쓰기를 **참/거짓**으로만 기록 | 실제 덮어쓸 줄의 **id**를 기록 |
| 새 저장 기간이 **화면에 안 보임** | '이전 시급 종료일'·'새 시급 종료일' 분리, 덮어쓰기 딱지(T-18-2) |
| 무배정 인력에 **파일의 근무지가 실제 근무지처럼** 표시됨 | '미배정'로 표시 |
| 사번 열 기본 빈칸 = 사용자 요구('사번 기준')와 불일치 | **머리글 자동 인식**으로 기본 적용, 못 찾으면 경고 |
| stale 배선·함수형 오류코드 시험 없음 | 적용 응답 처리를 `resolveWageBulkApplyAnswer`로 떼어 시험, helper 시험 추가 |

**R4에서 더 나온 4건**

| 무엇 | 어떻게 고쳤나 |
|---|---|
| 지문 불일치로 **적용이 거부돼도** 무효가 된 미리보기와 [적용] 버튼이 화면에 남음 | 전용 오류 코드로 판별해 미리보기를 폐기 → 다시 만들기 전엔 적용 불가 |
| 지문이 **저장 기간을 정하는 미래 시급**을 안 담음(적용 사이에 미래 시급이 생기면 종료일이 달라짐) | 저장 계획(새 줄 종료일·같은 시작일 덮어쓰기 여부)을 미리보기 행에 넣어 지문에 포함 |
| 퇴사일 동률 행의 **안내 문구**가 판정과 다름 | "적용일에는 이미 퇴사 처리된 인력입니다."로 |
| DB 변경(미래 시급 추가·중간 퇴사) 회귀 시험 없음 | 두 건 추가 |

**R7에서 더 나온 4건 + 실앱 대조에서 1건**

| 무엇 | 어떻게 고쳤나 |
|---|---|
| 미리보기·적용을 **시작한 뒤** 도착한 머리글 응답이 열 칸을 덮음(같은 유형 네 번째) | 세대 증가를 호출부에서 빼고 **매핑 수명주기 전체를 리듀서 하나**(`reduceWageBulkMapping`)로 옮김. 시험이 이벤트를 직접 흘려보냄 |
| 모호한 머리글 칸에 **기본값 D를 채움**(사실상 위치 추측) | 모호한 칸은 **빈칸**, 필수 칸이 비면 [미리보기] 잠금 |
| 단건 저장이 겹친 과거 이력 여러 줄 중 **한 줄만** 끝난다고 안내 | `previousRates`로 걸치는 줄 전부, 일괄과 같은 규칙 |
| 실앱: 배정 없는 인력의 근무지 칸이 **'미배정'이 아니라 빈칸** — `?? "미배정"`이 빈 문자열을 통과시킴 | 조회는 빈 값을 undefined로, 표시는 `||`로. **코드만 읽고 고쳤다고 단정한 결함** |

**R8에서 더 나온 5건**

| 무엇 | 어떻게 고쳤나 |
|---|---|
| 기본값 B/C/D가 완전한 매핑이라 **머리글을 읽는 동안에도 [미리보기]가 살아 있음** — 바로 누르면 확인 안 된 열로 미리보기하고 자동 인식 응답은 버려짐 | 매핑 모델에 **읽는 중/읽음/실패** 상태. 읽는 동안 [미리보기] 잠금("머리글 확인 중..."), 실패는 조용히 버리지 않고 "기본값을 넣었으니 확인하라"고 말함. 실앱 캡처가 잠금 순간을 잡음 |
| 열 하나를 손으로 고치면 **아직 사실인 다른 경고**(사번 없음)까지 사라짐 | 안내를 문장 하나가 아니라 **사실 목록**(`headerFacts`)으로 보존하고, 고친 칸의 문장만 물림(`describeWageBulkMapping`) |
| 선택 열인 사번만 중복일 때 "직접 넣어야 미리보기를 만들 수 있다"고 **버튼과 모순되는 안내** | 사번 전용 문구: "비워 두면 근무지명+이름으로 찾습니다. 사번으로 찾으려면 한 열을 지정" |
| 리듀서 시험은 화면이 이벤트를 **안 보내도** 통과 / 단건 다중 절단 회귀 시험 없음 | 모달의 상태·동작 전부를 `useWageBulkUpdate` 훅으로 떼고, **가짜 브리지로 훅을 화면처럼 모는 jsdom 시험**(머리글 응답을 시험이 원할 때 도착시킴). 겹친 이력 2줄·같은 날짜 덮어쓰기 시험 추가 |
| 날짜 박힌 조사 보고서가 `docs/`에 들어감(문서 보존 규칙 위반) | `artifacts/reviews/`로 이동, README·지도에 위치 명시 |

**R9에서 더 나온 6건**(R8 지적 5건과 ①②③ 묶음 전체 검증)

| 무엇 | 어떻게 고쳤나 |
|---|---|
| 입사일·퇴사일을 고쳐도 **이미 파싱된 승인대기 파일은 다시 읽지 않음** | 저장 시 실제 바뀐 경우에만 재분석 표시를 남기고, 다음 실적 관리 조회가 승인대기 파일을 한 번 다시 읽음(대체수당 정책과 같은 방식). 완료창 안내. **전제 정정**: 유일 후보·사번 힌트 행은 입사일 이전이어도 그대로 매칭·지급하므로(T-22) 재분석이 그 행을 "부적격"으로 만들지는 않는다. (R10에서 다시 정정: 동명이인 중 가용 후보가 있으면 입·퇴사일이 후보를 줄인다 — T-12 표) |
| T-2 뒤 **승인 행이 새 파싱 시급을 '승인' 상태로 표시**(지급은 옛 시급) | 승인 판정이 유지되는(`satisfied`) 행은 승인 스냅샷을 표시. 시험: 부분 승인 → 시급 변경 → 강제 재분석 → 상태·표시·계산 세 값 고정 |
| T-12 경고가 **덜 위험한 방향만** 말함 / main 검사가 화면 범위와 다름(2/30·99/99·1980·미래 통과) | 양방향 경고(빠르면 무시급 구간 날짜) · 공용 검사 함수 `employee-dates.ts`를 화면·main 양쪽이 사용 |
| T-19가 **직전 줄만 비교**해 중첩 겹침을 공백으로 오판 | 누적 범위 끝을 끌고 가는 스윕으로 재작성, 중첩 2·같은 시작일 3개 시험 |
| T-21 **PDF 품의서 인쇄일**에 UTC 잔존 | 내보내기가 로컬 인쇄일을 한 번 정해 PDF에 전달, 로컬 00:30 고정 시험 |
| T-20 신규 인력·배정·첫 시급이 **한 트랜잭션이 아님** | BEGIN/COMMIT/ROLLBACK, 트리거로 마지막 쓰기를 실패시키는 시험 + 재등록 성공 확인 |

**R10에서 더 나온 5건**(R9 수정분 검증)

| 무엇 | 어떻게 고쳤나 |
|---|---|
| main 검사가 **입사일 없음·빈 문자열, 퇴사 상태인데 퇴사일 없음, 재직인데 퇴사일 있음**을 통과시킴(검사는 빈칸을 건너뛰고 저장은 `""`를 넣음) | IPC 계약의 입사일을 필수로, 저장 시작부에서 날짜를 한 번 정규화해 검사·저장 모두 그 값 사용. 세 조합 거절 + 거절 시 행 불변 시험 |
| **신규 인력 등록**이 승인대기 파일을 다시 읽게 하지 않음 — "파싱 → 인력 없음 오류 → 등록 → 복귀"해도 오류 그대로. 시험이 반대 전제를 고정 | 신규 등록·이름·사번·입·퇴사일·배정 저장/해지를 **하나의 '인력 기본정보 재분석' 규칙**으로 묶음. 시험을 "오류 파일 저장 → 등록 → 일반 조회 → 저장된 행의 사번·시급 해결"로 교체 |
| R10 전제 정정이 **동명이인 분기를 빠뜨려** 규칙 문서를 과잉 일반화 | T-12 분기표 4행 + 동명이인 두 명의 기간을 바꿔 매칭 사번이 바뀌는 파서 시험. **T-22는 경고로 구현**(차단은 사용자 결정) |
| T-19 같은 시작일 + 앞 구간 겹침이 한 줄에 겹치면 **React key 중복** / 요약이 문장 수를 셈 | 안내에 종류 코드·고유 id·원인 키. 화면 key = id, 요약 = 원인 수(`summarizeWageHistoryIssues`). 복합 사례 시험 |
| 재분석 표시를 **다시 읽기 전에 지움**(실패하면 재시도 없음) / 날짜 저장과 표시 기록이 **원자적이지 않음** | 표시를 토큰으로: 저장 트랜잭션 안에서 기록, 조회는 peek → 다시 읽기 → 읽었던 토큰으로만 ack. 파일 표를 잠시 없애 첫 조회를 실패시키고 두 번째 정상 조회가 소비하는 시험 + 토큰 교차 시험 |

**R11 자체 적대검증에서 더 나온 3건**(Codex 한도 초과로 Claude가 직접 검증)

| 무엇 | 어떻게 고쳤나 |
|---|---|
| **연·월을 고른 조회가 그 달 파일만 다시 읽고 표시를 지움** — 실적 화면은 거의 항상 달을 고른 채 조회하고, 수당 화면의 승인완료 조회도 같은 표시를 소비하므로 **다른 달 승인대기 파일은 옛 인력 정보로 남았다**(R9·R10 구현 모두 같은 결함, 대체수당 정책 표시도 동일) | 달 조회는 토큰 아래 "읽은 달"만 기록(`recordReparseMonth`), 같은 달은 다시 안 읽음(`isReparseMonthCovered`), 새 토큰이면 기록 초기화, **전체 기간 조회만 ack**. 시험: 승인완료+달 조회 → 그 달만 새 시급·표시 유지 → 같은 달 재조회 안 읽음 → 전체 기간 조회 → 표시 소비 |
| **근무지 이름 변경·삭제**가 재분석 규칙에서 빠짐 — 파서는 파일의 근무지 이름으로 근무표를 찾고 동명이인을 좁힌다 | 근무지 저장(이름이 실제로 바뀔 때)·삭제를 트랜잭션으로 감싸고 표시를 안에서 기록. 시험 추가 |
| T-22 퇴사 문구가 **퇴사 처리일 당일**(파서는 당일부터 비가용)을 "이후"라고만 말함 | "퇴사 처리일(…) 당일이거나 그 뒤입니다"로 정정 |

**R13 자체 적대검증 2차(영역: 단건 저장 알고리즘·현재 시급 판정 일관성·일괄 적용 경로·승인 수동 시급·파서 시급 경계)**

| 무엇 | 어떻게 고쳤나 |
|---|---|
| **같은 적용일로 저장하면 앞줄을 끊지 않음** — 옛 프로그램에서 넘어온 겹친 이력("1/1~계속" 아래 "7/1~계속")은 T-19 안내가 "그 날짜(7/1)로 저장하면 정리된다"고 말하지만 7/1 저장은 같은 날짜 분기라 1/1 줄이 열린 채 남아 **겹침이 영영 안 풀림**. 화면 미리보기·일괄 계획도 "덮어쓰기는 아무것도 안 끊는다"로 같은 규칙을 박아 두었고 시험이 그 상태를 고정 | 앞줄 끊기를 두 분기 공통으로(정상 이력에선 끊을 줄이 없어 동작 변화 없음). 화면 미리보기(`buildWageSavePreview`)·일괄 계획(`buildSavePlansByEmployee`)·일괄 표시(`describeTruncation`)를 같은 규칙으로. 같은 날짜 안내문에 "걸치는 앞줄도 전날에 끝나 겹침이 정리됩니다" 추가. 시험 3곳 교체·추가 |
| T-19 요약이 "같은 시작일 줄"까지 그 날짜로 저장하면 정리된다고 약속 — 같은 시작일 여러 줄은 지울 수 없어(R-13) 가장 나중 줄만 쓰일 뿐 안내가 계속 남음 | 요약 문장을 "앞 줄과 겹치거나 사이가 빈 기간은 정리되고, 같은 시작일 줄은 가장 나중에 만든 줄만 쓰이며 지우지 않는다"로 정정 |

공격했지만 결함이 아니었던 것: 현재 시급 판정(목록 SQL·`listStoredEmployeeWageRates`·파서·화면·일괄 조회 전부 `effective_from DESC, created_at DESC`로 동일) · 일괄 적용(한 트랜잭션, 같은 저장 함수, 파일 내 중복 인력은 첫 행만, 미리보기 지문 재확인) · 승인 수동 시급(알림 걷어내기는 문구에 '시급'이 있어야 하므로 T-22 경고는 남음) · 퇴사자 판정(퇴사 처리일 ≤ 적용일이면 제외, 없으면 상태) · 파서 시급 경계(당일 포함 시험 있음).

**R14 자체 적대검증 3차(영역: 인력 상세 화면 흐름 · 근무표 편성의 입사일 관문 · 품의서/별첨 시급 표기 · 이관 자료의 빈 입사일)**

| 무엇 | 어떻게 고쳤나 |
|---|---|
| 인력 상세의 **기본 정보 칸이 관련 없는 새로고침에 초기화됨** — 시급 저장·배정 해지·일괄 적용 뒤 목록을 다시 읽으면 값은 같은데 새 객체가 오고, 폼이 객체 기준으로 초기화돼 **타이핑 중이던 연락처·상태·입사일이 사라짐**. 시급 칸은 R8에서 "값 기준"으로 고쳤는데 기본 정보 칸은 그대로였다 | 기본 정보 폼을 `employee-detail-form.ts`로 떼고, 폼이 비추는 저장값 6개(고용형태·직급·연락처·상태·입사일·퇴사일)가 바뀌거나 사람이 바뀔 때만 초기화. 시험 3건(시급·배정·타임스탬프만 바뀐 새 객체 = 같은 키) |
| 옛 자료의 **빈 입사일**: 머리글은 배정 시작일을 대신 보여 주고(R-19) 폼은 비어 있어 "왜 저장이 막히는지" 안 보임 | 폼 아래 안내 한 줄(옛 자료·저장하려면 입사일 필요·위 표시는 배정 시작일) |

공격했지만 결함이 아니었던 것: 품의서·별첨의 시급은 승인 시점 계산 결과(`result.hourlyRate`)만 쓴다(현재 시급 아님) · 이관(R-20)은 입사일을 항상 채운다(빈 입사일은 아주 옛 자료뿐) · 인력 삭제는 퇴사 처리일이 지난 퇴사자만.

**R15 사용자 결정 반영(2026-09-05)** — R14가 올린 결정 항목 두 개에 사용자가 답했다: "배정 시작일은 입사일보다 앞설 수 없다. 입사일은 시급의 기준, 배정일은 근무표의 기준. 시급 적용일도 입사일보다 앞이면 막아라" / "고용 기간 밖 근무는 승인되면 안 된다".

| 사용자 결정 | 반영 |
|---|---|
| 배정 시작일 ≥ 입사일 | 저장 거절 + 마법사 2단계 드롭 시 즉시 안내(**T-23**) |
| 시급 적용일 ≥ 입사일(인력 상세) | 저장 거절 + 달력에서 입사일 이전 회색 처리 + 일괄 적용은 "입사 전 제외"로 따로 빼기(**T-23**) |
| 근무표 편성 시작일 | 입사일과 배정 시작일 중 **늦은 날**(편성·근무표 화면). 파서는 배정 이력 전체로 판단하므로 그대로(**T-23**) |
| 고용 기간 밖 근무 승인 | 경고 → **오류(승인 차단)**, 시급 임의지정으로도 못 넘김. 푸는 길 = 인력 관리에서 날짜 정정 → 자동 다시 읽기(**T-22**) |

사용자가 함께 신고한 "인력 상세에서 시급 적용일이 오늘 이전으로 안 잡히고 다음날로 저장된다"는 **배포판 0.5.4의 T-5(현재 적용일+1 기본값)·달력 '확인' 되돌림 결함**으로, 이 브랜치에서 이미 고쳐져 있다(적용일 기본값 = 오늘, 달력은 고른 즉시 확정, 실앱 하네스가 같은 날짜 저장을 관측). 게시 전까지는 배포판에서 계속 재현된다.

**시험 하네스**: 모달의 동작은 `useWageBulkUpdate.test.tsx`(jsdom, 가짜 브리지)로, 실제 화면은 `artifacts/scripts/capture-wage-bulk-modal.cjs`(빌드본 구동, 인력 상세 3장 + 모달 7장 + 화면에서 읽은 값 JSON — 같은 날짜 저장 전후, 연락처 유지, 시급 적용일 달력의 입사일 이전 회색 처리와 막힌 날 클릭 무반응까지 관측)로 본다. 화면 컴포넌트 전체를 렌더하는 시험은 여전히 없다.

**R16 자체검증(R15 구현분) + 조 근무유형 재분석(2026-09-05)**: R15 편지의 각도 4개를 공격했으나 결함 없음 — ① 오류 알림 행은 "확정 대상"(`getEligibleApprovalEntries`는 Pool 대체 행만 뺀다)에 남아 단건 승인·재승인 마무리·자동 보관 모두 막힌다(우회 없음) ② 규칙 이전 자료(배정<입사)는 편성·근무표 화면이 같은 함수(`getEmployeeScheduleStartDate`)를 쓰므로 같은 날을 가리키고, 재배정은 오늘≥입사일이면 통과 ③ 일괄 지문은 전체 행(상태·안내 포함)을 해시하므로 "입사 전 제외" 행과 입사일 변경이 모두 지문을 바꾼다 ④ `hireDate ??` 전수 grep — 남은 곳은 파서 가용 판정(의도)·R-19 표시 폴백뿐. 이어서 **조 근무유형 변경 시 재분석**을 구현했다(위 T-12 단락). 끝에서 끝까지 시험: 정책 시작일 설정 → 파일 읽기(대체 행 지급) → 근무 설정에서 C조를 Pool로 저장 → 강제 새로고침 없이 다음 조회가 다시 읽어 대체 행이 미지급으로 바뀜(`performance-management-service.test.ts` "reads the pending files again once after a team's work type changes in the shift settings"). 화면 완료창에는 아직 안내가 없다(조회 시 진행 표시만).

**R17 남은 두 가지 마감(2026-09-05, 사용자 지시)**: ① 근무 설정 저장 결과에 `teamWorkTypeChanged`를 실어(`ShiftPatternSaveResult`) 마법사의 다음 완료창이 안내를 한 번 보여 준다(화면은 표시를 참조에 담아 두고 1단계 적용 완료·2단계 저장 완료·배정 저장 완료 중 먼저 뜨는 창이 가져간다). ② **근무표 재생성 빈틈**: 월간 근무표 저장이 표시 종류 4번째 '근무표'를 같은 트랜잭션 안에서 남기고, 다음 실적 관리 조회가 승인대기 파일을 다시 읽는다. 끝에서 끝까지 시험: 파일 읽기 → 시급 저장(다시 읽기 전엔 안 보임, T-1) → 조회(그대로) → 같은 달 근무표를 같은 내용으로 재저장 → 새 토큰 → 조회가 다시 읽어 새 시급이 보임(`performance-management-service.test.ts` "reads the pending files again once after the month's schedule is saved again"). 시작 시 복구(`monthly-schedule-restore-service`)의 근무표 저장도 같은 함수를 거쳐 표시를 남기지만, 복구는 이미 `restoredScheduleCount > 0`이면 강제 다시 읽기를 하므로 중복이어도 한 번만 읽는다.

**아직 남은 것**: T-14(화면에 종료 기능이 없어 지금은 도달 불가; T-19 표시로 공백은 보임) · T-15(이관 데이터 — 코드가 아니라 운영 PC 진단) · 게시(모든 기능 검증이 끝나면 사용자가 지시).
