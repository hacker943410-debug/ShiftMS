# 운영 참고서

## 문서 역할
- 이 문서는 `DB업데이트`, 수당 계산, 양식 관리처럼 운영 기준이 필요한 항목만 모아 둔 요약 문서다.
- 실운영 검수 기록은 `docs/operations-manual-qa-checklist.md` 에 남기고, 릴리즈 판단은 `docs/release-0.1.0.md` 에 반영한다.

## 1. `DB업데이트` / Access 마이그레이션 기준

### 대상 원본
- 기본 원본: `양식샘플/DT사업1팀_교대근무관리DB.accdb`
- 연결 방식: `Microsoft.ACE.OLEDB.16.0` 우선, 실패 시 `12.0`
- JSON 백업 파일도 같은 `DB업데이트` 흐름으로 복원 가능

### 현재 복원 범위
- 공휴일
- 요율
- 근무지
- 인력
- 시급
- 패턴
- 배정 종료일
- `사업조직별근무실적` 기반 기본 승인/수당 이력

### 현재 복원 비포함
- 양식 이력
- 출력 이력
- 원본 Access 파일 자체 재생성

### 핵심 매핑 규칙
1. `공휴일` 은 연도별 전체 교체 방식으로 반영한다.
2. `연장근로요율` 은 앱 기본값이 아니라 Access 원본값을 그대로 저장 기준으로 쓴다.
3. `사업조직별근무자현황` import 시 빈 슬롯 행(`None_*`, `공석`, `TBD`, 무사번`)은 제외한다.
4. `근무자별시급관리` 는 1차 기준으로 `적용유무=True` 인 최신 활성 시급을 우선 반영한다.
5. `직무해제자현황` 은 `retireDate` 가 아니라 `employee_site_assignments.end_date` 로만 반영한다.
6. `사업조직별근무실적` 은 `근무지 + YYYY-MM + 승인상태` 기준 synthetic file 로 변환해 반영한다.
7. Pool 대체근무는 기록에는 남기되 화면/수당 대상에서는 제외한다.
8. `A / B / C` 같은 조명은 현재 앱에서 `A조 / B조 / C조` 로 정규화한다.

### 현재 제한
1. `SK증권 홍길동1 2024-08-15` 1건은 시급을 복원하지 못해 승인/수당 이력을 생성하지 않는다.
2. `대전DC`, `대전NOC`, `울산CLX` 패턴은 원본 시간 슬롯 부재로 제외한다.
3. 운영 PC 권한 정책, 백신 예외, 네트워크 폴더 정책은 별도 현장 검증이 필요하다.

### 권장 실행 순서
1. 경로 설정 저장
2. JSON 기준 `DB업데이트` 확인
3. Access 기준 `DB업데이트` 확인
4. 공휴일 / 요율 / 인력 / 패턴 / 승인 기본 이력 sanity check

## 2. 수당 요율 / 계산 기준

### 요율 구조
수당 요율은 `근로유형 5행 x 기본/연장/야간 3열` 구조로 관리한다.

| 근로유형 | 기본 | 연장 | 야간 |
| --- | --- | --- | --- |
| 법정공휴일 | 1.5 | 1.5 | 1.5 |
| 평_대체근로수당 | 1.5 | 2.0 | 2.5 |
| 휴_대체근로수당 | 1.5 | 2.0 | 2.5 |
| 평_연장근로수당 | 0 | 1.5 | 2.0 |
| 휴_연장근로수당 | 0 | 0 | 0 |

### 시간 분해 기준
1. 야간근로시간: `22:00 ~ 06:00`, 휴게시간은 야간에서 우선 차감
2. 기본근로시간: 비야간 근로시간 중 최대 8시간
3. 연장근로시간: 기본근로시간 8시간을 채우고 남은 비야간 근로시간
4. `기본 + 연장 + 야간 = 총근로시간` 이 되도록 유지

### 근로유형 매핑
- `법정공휴일`, `공휴일`, `휴일근로` -> `법정공휴일`
- `평_대체`, `대체근로` -> `평_대체근로수당`
- `휴_대체` -> `휴_대체근로수당`
- `평_연장`, `연장근로` -> `평_연장근로수당`
- `휴_연장` -> `휴_연장근로수당`

### 출력/집계 규칙
- `법정공휴일` -> 법정공휴일수당
- `평_대체근로수당`, `휴_대체근로수당` -> 대체근로수당
- `평_연장근로수당`, `휴_연장근로수당` -> 연장근로수당
- 화면/별첨 표기는 `대체근무`, `연장근무`, `법정휴일근무` 라벨을 사용한다.

### 예외 규칙
- Pool 근무자의 대체근무는 실적 기록에는 남기지만 수당 계산/집계/품의 출력에서는 제외한다.
- 임의 시급 재승인 행은 최신 승인 스냅샷의 시급을 우선 사용한다.
- 퇴직자 선지급 건은 일반 지급 합계와 품의서 2번 항목에서 제외하고, 품의서 3번 항목으로 별도 집계한다.

## 3. 양식 관리 기준

### 지원 양식
- 근무표 양식 1: `근무표_템플릿1.xlsx`, `sample1`, 일반형
- 근무표 양식 2: `근무표_템플릿2.xlsx`, `sample2`, 6조 2교대형
- 품의서, 별첨1, 별첨2는 같은 승인형 등록 흐름을 사용한다.

### 공통 원칙
1. 운영 중인 양식 파일을 직접 덮어쓰지 않는다.
2. 양식 수정은 항상 `새 파일 + 새 버전`으로 등록한다.
3. 승인된 양식만 실제 배포/출력 메뉴에서 선택된다.
4. 이미 저장된 산출물은 저장 당시의 `templateVersionId` 를 계속 사용한다.
5. 구조가 달라지면 새 계열로 보고 preview/export 알고리즘과 테스트를 같이 수정한다.

### 운영 흐름
1. 파일 Import
2. 1차 검증
3. 프로필/좌표 확인
4. 미리보기
5. `미승인` 저장
6. 운영 담당자 승인
7. `기본 사용` 전환

### 최소 검증
- `inspectSchedulePlanTemplate`
- `previewMonthlySchedulePlan`
- `exportMonthlySchedulePlan`
- `publishSchedulePlanExport`
- `npm run typecheck`

### 수동 확인
- 사이트명
- 기준월
- 팀 요약
- 좌측 날짜/근무코드
- 중앙 정규 계획
- 변경 계획/사유 비움 상태
