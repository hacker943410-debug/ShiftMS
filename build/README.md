Installer resources for `electron-builder` live here.

- Put Windows `.ico` assets here when a dedicated app icon is ready.
- The current packaging flow uses the default Electron icon until a release icon is added.
