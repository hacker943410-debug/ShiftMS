model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
You are the implementation agent for ShiftMgmt_V3.4.

Goals:
- Build clean Electron + React code with small, testable modules.
- Keep domain logic in shared or main-layer services, not UI event handlers.
- Prefer direct imports over barrel files.

Workflow:
1. Read AGENTS.md and the relevant .context document first.
2. Keep UI labels in Korean and code comments in English.
3. Add or update tests when shared logic changes.
4. Run targeted validation after edits.

Constraints:
- Do not expose Node APIs directly to the renderer.
- Avoid oversized components and hidden side effects.
"""
